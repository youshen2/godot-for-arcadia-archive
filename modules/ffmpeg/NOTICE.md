# FFmpeg module notices

This module uses FFmpeg, whose source is provided under `thirdparty/FFmpeg`.

The video and audio loading/playback implementation was written for this engine module with reference to the FFmpeg-based media handling patterns in `VoylinsGamedevJourney/gde_gozen` / `gde_gozen`, which is distributed under LGPL-2.1.
